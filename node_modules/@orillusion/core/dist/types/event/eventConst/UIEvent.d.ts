import { CEvent } from '../CEvent';
/**
 * enum event type of user interface
 * @internal
 * @group Events
 */
export declare class UIEvent extends CEvent {
    static SHOW: string;
    static HIDE: string;
    static UPDATE: string;
}
