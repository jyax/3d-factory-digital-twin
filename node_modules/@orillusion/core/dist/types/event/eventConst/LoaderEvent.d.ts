import { CEvent } from '../CEvent';
/**
 * enum loader event
 * @internal
 * @group Events
 */
export declare class LoaderEvent extends CEvent {
    static LOADER_PROGRESS: string;
    static LOADER_COMPLETE: string;
}
