import { RenderTexture } from "../../../textures/RenderTexture";
import { RTFrame } from "./RTFrame";
export declare class GBufferFrame extends RTFrame {
    static gBufferMap: Map<string, GBufferFrame>;
    static bufferTexture: boolean;
    constructor();
    crateGBuffer(key: string, rtWidth: number, rtHeight: number): void;
    getColorMap(): RenderTexture;
    getPositionMap(): RenderTexture;
    getNormalMap(): RenderTexture;
    getMaterialMap(): RenderTexture;
    /**
     * @internal
     */
    static getGBufferFrame(key: string): GBufferFrame;
    clone(): GBufferFrame;
}
