import { Color } from "../../../../math/Color";
import { Vector3 } from "../../../../math/Vector3";
/**
 * @internal
 */
export declare class Graphics3DShape {
    uuid: string;
    type: string;
    color: Color;
    count: number;
    pointData: Float32Array;
    colorData: Float32Array;
    dirtyData: boolean;
    memoryDataIndex: number;
    protected transformIndex: number;
    constructor(transformIndex: number);
    buildAxis(origin?: Vector3, size?: number): void;
    buildLines(points: Vector3[], colors?: Color | Color[]): void;
    buildArcLine(center: Vector3, radius: number, startAngle: number, endAngle: number, segments?: number, up?: Vector3, color?: Color): void;
    buildCircle(center: Vector3, radius: number, segments?: number, up?: Vector3, color?: Color): void;
    fillShapeData(points: Vector3[], colors: Color | Color[], forceUpdate?: boolean): void;
    reset(): void;
}
